Placeholders so the project-local Z3 layout exists in version control.
The actual content of source/, build/ and install/ is generated by
scripts/bootstrap_z3.ps1 / scripts/bootstrap_z3.sh and must not be committed.
